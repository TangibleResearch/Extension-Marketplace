# Tangible Studio Extension Tools

Create a starter extension:

```sh
python -m tangible_extensions.creator "My Extension" --language typescript
```

Each generated extension includes a `tangible-extension.json` manifest, command contribution, and starter activation file.
